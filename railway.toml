[build]
builder = "nixpacks"
buildCommand = "./mvnw -DskipTests package"

[deploy]
startCommand = "java -jar target/sprint-planning-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
